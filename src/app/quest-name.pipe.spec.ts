import { QuestNamePipe } from './quest-name.pipe';

describe('QuestNamePipe', () => {
  it('create an instance', () => {
    const pipe = new QuestNamePipe();
    expect(pipe).toBeTruthy();
  });
});
